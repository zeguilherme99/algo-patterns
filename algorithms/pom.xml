<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.zeguilherme99</groupId>
  <artifactId>algo-patterns</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>algo-patterns</name>
  <description>Reference implementations of algorithm patterns that record step-by-step traces for visualization.</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jackson.version>2.17.2</jackson.version>
    <junit.version>5.10.3</junit.version>
    <!-- Practice tests are excluded by default; run them with: mvn test -Ppractice -->
    <surefire.excludedGroups>practice</surefire.excludedGroups>
    <surefire.groups></surefire.groups>
    <traces.outputDir>${project.basedir}/../web/public/traces</traces.outputDir>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <excludedGroups>${surefire.excludedGroups}</excludedGroups>
          <groups>${surefire.groups}</groups>
        </configuration>
      </plugin>
      <!-- mvn exec:java  -> regenerates web/public/traces/*.json -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <mainClass>io.github.zeguilherme99.algopatterns.trace.TraceExporter</mainClass>
          <arguments>
            <argument>${traces.outputDir}</argument>
          </arguments>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>practice</id>
      <properties>
        <surefire.excludedGroups></surefire.excludedGroups>
        <surefire.groups>practice</surefire.groups>
      </properties>
    </profile>
  </profiles>
</project>
